[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "akim-na-5-chasov-ai"
version = "0.1.0"
description = "AI analysis module for the Аким на 5 часов simulator"
requires-python = ">=3.10"
dependencies = []
